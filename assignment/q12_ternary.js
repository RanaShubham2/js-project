
// Q10. Write a function classify(n) that returns 'positive', 'negative', or 'zero' using only the ternary operator
// (no if-else).
function ternary(n){
 console.log(n>0?'number is positive':n<0?'number is negative':'zero');

}
ternary(0)
